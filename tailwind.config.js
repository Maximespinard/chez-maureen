/**
 * Configuration Tailwind CSS minimale pour compatibilité ESLint
 *
 * Note: Le projet utilise Tailwind v4 avec configuration CSS-based (src/styles.css).
 * Ce fichier existe uniquement pour que eslint-plugin-tailwindcss@beta puisse fonctionner.
 * La vraie configuration se trouve dans src/styles.css via @theme inline.
 */

export default {}
